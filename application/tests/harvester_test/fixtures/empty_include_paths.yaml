repositories:
  - id: asvs
    type: github
    owner: OWASP
    repo: ASVS

    paths:
      include: []

    chunking:
      strategy: markdown_heading
      max_tokens: 1200

    polling:
      mode: incremental
      interval_minutes: 60
